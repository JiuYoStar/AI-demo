# **最常用 Git Commit 类型**

| 类型         | 说明                     | 示例                                       |
| ------------ | ------------------------ | ------------------------------------------ |
| **feat**     | 增加新功能               | feat: 支持 RAGFlow 文档批量上传            |
| **fix**      | 修复 bug                 | fix: 修复 markdown-it-katex 顶出输入框问题 |
| **docs**     | 文档修改                 | docs: 更新 API 文档                        |
| **style**    | 不影响逻辑的代码格式修改 | style: 调整 eslint 规则                    |
| **refactor** | 重构（无新功能、无修复） | refactor: 重写向量召回模块结构             |
| **perf**     | 性能优化                 | perf: 优化 context-window chunk 合并速度   |
| **test**     | 单元测试或 e2e 测试      | test: 增加 embedding normalize 的测试      |
| **build**    | 构建系统、依赖修改       | build: 升级 Vite 到 v5                     |
| **ci**       | CI/CD 配置变更           | ci: 优化 GitHub Action 缓存                |
| **chore**    | 杂项（不改代码逻辑）     | chore: 更新依赖锁文件                      |
| **revert**   | 撤销 commit              | revert: 回滚 fix infinite loop 代码        |

---

# 🔥 **前端工程师常用的扩展关键词（社区常见）**

| 类型                    | 说明                                       |
| ----------------------- | ------------------------------------------ |
| **wip**                 | Work in Progress，未完成功能（你现在在用） |
| **init**                | 初始化项目                                 |
| **release**             | 发布版本                                   |
| **merge**               | 合并分支                                   |
| **hotfix**              | 紧急修复（线上问题）                       |
| **upgrade / downgrade** | 升级/降级依赖版本                          |
| **security**            | 安全相关（XSS、权限等）                    |
| **deps**                | 单纯更新依赖（更轻量版 build/chore）       |

---

# 🚀 **AI 工程师常用扩展（针对模型 / RAG / Agent 项目）**

| 类型         | 使用场景                              |
| ------------ | ------------------------------------- |
| **model**    | 模型下载、切换、权重调整              |
| **data**     | 数据修改：embedding、raw docs、训练集 |
| **prompt**   | 提示词更新（Agent/LLM）               |
| **rag**      | RAGFlow / 知识库相关的更新            |
| **agent**    | Agent 工作流 / 功能调整               |
| **workflow** | pipeline / chain 更新                 |
| **infra**    | GPU、vLLM、Ollama 等部署相关          |

示例：

```
rag: 调整 chunk overlap 为 80 → 120
prompt: 优化香港POC分类提示词
agent: 新增审批流程动作节点
infra: 调整 vLLM kv-cache 配置
```

---

# 🎯 **实际推荐组合写法**

我知道你平时 commit 风格是：

```
fix:
feat:
wip:
```

你可以继续这么用，但如果想更规范又更适合大型项目，可以这样组合：

### 单类 + scope（推荐）

```
feat(web): 支持右侧 outline 动态折叠
fix(rag): 修复向量召回 precision 偏低的问题
perf(embedding): 优化 embedding 生成速度
```

### 多类型组合

```
fix(web): 修复editor聚焦顶出问题 & 优化布局计算
```

### WIP 继续保留

```
wip(agent): 重写连线关系递归逻辑
```

---

# 🧩 最终汇总（全类别快速列表）

### **核心（标准）**

feat / fix / docs / style / refactor / perf / test / build / ci / chore / revert

### **前端常用**

wip / init / release / merge / hotfix / deps / upgrade / downgrade / security

### **AI / 大模型项目扩展**

model / data / prompt / rag / agent / workflow / infra / eval
