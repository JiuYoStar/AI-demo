## 🧠 Python 常见数据类型增删改查 + 判断速查表

| 类型            | 操作   | 方法 / 语法                                         | 示例                             | 说明       |
| ------------- | ---- | ----------------------------------------------- | ------------------------------ | -------- |
| **`str` 字符串** | 查    | `s[index]`, `s[start:end]`                      | `"abc"[1] → 'b'`               | 取字符或子串   |
|               | 改    | 不可变 ❌                                           | -                              | 只能创建新字符串 |
|               | 增    | `+`, `join()`                                   | `'a' + 'b'` → `'ab'`           | 拼接       |
|               | 删    | `replace()`                                     | `'abc'.replace('b','') → 'ac'` | 替换为空即删除  |
|               | 判断存在 | `sub in s`                                      | `'a' in 'cat' → True`          | 判断子串是否存在 |
|               | 其他   | `split()`, `strip()`, `upper()`, `startswith()` | `' abc '.strip()` → `'abc'`    | 常用处理函数   |

---

| 类型           | 操作    | 方法 / 语法                                 | 示例              | 说明     |
| ------------ | ----- | --------------------------------------- | --------------- | ------ |
| **`int` 整数** | 查     | -                                       | -               | 原子类型   |
|              | 改 / 增 | `+`, `-`, `*`, `/`, `//`, `%`, `**`     | `x += 1`        | 算术运算   |
|              | 删     | 不适用 ❌                                   | -               | 无法删除   |
|              | 判断存在  | 不适用 ❌                                   | -               | 非容器类型  |
|              | 其他    | `abs()`, `round()`, `divmod()`, `pow()` | `abs(-3)` → `3` | 常见数学操作 |

---

| 类型            | 操作   | 方法 / 语法                                       | 示例                    | 说明       |
| ------------- | ---- | --------------------------------------------- | --------------------- | -------- |
| **`list` 列表** | 查    | `lst[i]`, `lst[start:end]`, `index(x)`        | `lst[1]`              | 取值或索引    |
|               | 改    | `lst[i] = x`                                  | `lst[0] = 99`         | 修改指定位置   |
|               | 增    | `append(x)`, `extend(seq)`, `insert(i, x)`    | `lst.append(5)`       | 添加元素     |
|               | 删    | `pop()`, `remove(x)`, `del lst[i]`, `clear()` | `lst.pop()`           | 删除元素     |
|               | 判断存在 | `x in lst`                                    | `3 in [1,2,3] → True` | 判断元素是否存在 |
|               | 其他   | `sort()`, `reverse()`, `copy()`               | `lst.sort()`          | 排序、复制    |

---

| 类型             | 操作        | 方法 / 语法                            | 示例                     | 说明       |
| -------------- | --------- | ---------------------------------- | ---------------------- | -------- |
| **`tuple` 元组** | 查         | `t[i]`, `t[start:end]`, `index(x)` | `(1,2,3)[0] → 1`       | 取值或索引    |
|                | 改 / 增 / 删 | 不可变 ❌                              | -                      | 无法修改     |
|                | 判断存在      | `x in t`                           | `2 in (1,2,3)` → True  | 判断元素是否存在 |
|                | 其他        | `count(x)`                         | `(1,1,2).count(1)` → 2 | 统计出现次数   |

---

| 类型           | 操作   | 方法 / 语法                                       | 示例                    | 说明               |      |
| ------------ | ---- | --------------------------------------------- | --------------------- | ---------------- | ---- |
| **`set` 集合** | 查    | 迭代或取长度                                        | `len(s)`              | 无序集合             |      |
|              | 改    | 无原地改                                          | -                     | 元素唯一             |      |
|              | 增    | `add(x)`, `update(seq)`                       | `s.add(4)`            | 添加元素             |      |
|              | 删    | `remove(x)`, `discard(x)`, `pop()`, `clear()` | `s.remove(2)`         | 删除元素             |      |
|              | 判断存在 | `x in s`                                      | `3 in {1,2,3}` → True | 判断成员是否存在         |      |
|              | 其他   | `union()`, `intersection()`, `difference()`   | `{1,2}                | {2,3}`→`{1,2,3}` | 集合运算 |

---

| 类型            | 操作   | 方法 / 语法                                           | 示例                       | 说明      |
| ------------- | ---- | ------------------------------------------------- | ------------------------ | ------- |
| **`dict` 字典** | 查    | `d[key]`, `get(k,default)`, `keys()`, `values()`  | `d['a']`, `d.get('x',0)` | 取值或遍历   |
|               | 改    | `d[key] = new_value`                              | `d['a'] = 99`            | 更新键值    |
|               | 增    | `d[key] = value`, `update({...})`                 | `d.update({'b':2})`      | 添加键值对   |
|               | 删    | `pop(key)`, `popitem()`, `del d[key]`, `clear()`  | `d.pop('a')`             | 删除键     |
|               | 判断存在 | `key in d`                                        | `'a' in {'a':1}` → True  | 判断键是否存在 |
|               | 其他   | `items()`, `copy()`, `setdefault()`, `fromkeys()` | `d.items()`              | 常用操作    |

---

## ⚡ 一眼速记总结

| 类型      | 可变性   | 判断语法       | 判断说明       |
| ------- | ----- | ---------- | ---------- |
| `str`   | ❌ 不可变 | `'a' in s` | 判断子串       |
| `int`   | ❌ 不可变 | 不适用        | 非容器        |
| `list`  | ✅ 可变  | `x in lst` | 判断元素是否在列表中 |
| `tuple` | ❌ 不可变 | `x in t`   | 判断元素是否在元组中 |
| `set`   | ✅ 可变  | `x in s`   | 判断元素是否在集合中 |
| `dict`  | ✅ 可变  | `k in d`   | 判断键是否在字典中  |

---
