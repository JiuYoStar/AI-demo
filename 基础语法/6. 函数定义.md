好的 ✅，我帮你整理了一份 **Python vs JavaScript 函数定义对比速查表**，涵盖常见用法和差异：

---

# 🐍 Python vs 🌐 JavaScript 函数定义对比

| 功能           | Python                                                     | JavaScript                         | 示例 (输出)            |
| -------------- | ---------------------------------------------------------- | ---------------------------------- | ---------------------- |
| **基本函数**   | `def foo():`                                               | `function foo() {}`                | 都能正常调用           |
| **带参数**     | `def add(a, b): return a+b`                                | `function add(a,b){ return a+b; }` | `add(2,3) → 5`         |
| **默认参数**   | `def greet(name=\"GG\"):`                                  | `function greet(name=\"GG\"){}`    | `greet() → GG`         |
| **可变参数**   | `def foo(*args):` (位置) <br>`def foo(**kwargs):` (关键字) | `function foo(...args){}` (展开)   | Python 多了 `**kwargs` |
| **匿名函数**   | `lambda x: x*2`                                            | `(x)=>x*2`                         | `2 → 4`                |
| **作用域**     | `global`, `nonlocal`                                       | `var`, `let`, `const`              | Python 显式声明作用域  |
| **返回多个值** | `return a, b` → 元组                                       | 只能 `return [a,b]` 或对象         | Python 更自然          |
| **函数即对象** | 函数是对象，可赋值                                         | 函数是一等公民                     | 都能当参数传递         |
| **闭包**       | 支持，需 `nonlocal`                                        | 支持，天然闭包                     | 用法接近               |
| **异步**       | `async def foo():` + `await`                               | `async function foo(){}` + `await` | 都支持 async/await     |

---

## 📌 示例对比

### Python

```python
# 基本函数
def add(a, b=10):
    return a + b

# 可变参数
def foo(*args, **kwargs):
    return args, kwargs

# 匿名函数
double = lambda x: x*2

# 异步函数
import asyncio
async def async_foo():
    await asyncio.sleep(1)
    return "done"

print(add(5))                # 15
print(foo(1, 2, x=3))        # ((1,2), {'x':3})
print(double(4))             # 8
```

### JavaScript

```javascript
// 基本函数
function add(a, b=10) {
    return a + b;
}

// 可变参数
function foo(...args) {
    return args;
}

// 匿名函数 (箭头函数)
const double = (x) => x*2;

// 异步函数
async function asyncFoo() {
    await new Promise(r => setTimeout(r, 1000));
    return "done";
}

console.log(add(5));           // 15
console.log(foo(1,2,3));       // [1,2,3]
console.log(double(4));        // 8
```

---

👉 总结：

* **Python** → `def`，支持 `*args`、`**kwargs`，返回多个值。
* **JavaScript** → `function` 或 `()=>`，可变参数 `...args`，返回单个值（数组/对象模拟多值）。
* **相同点**：都支持默认参数、匿名函数、闭包、异步。



```python
def demo(a, b=10, *args, **kwargs):
    print("a =", a)
    print("b =", b)
    print("args =", args)
    print("kwargs =", kwargs)

demo(1, 2, 3, 4, x=5, y=6)

```打印结果```
a = 1
b = 2
args = (3, 4)
kwargs = {'x': 5, 'y': 6}

指定变量名的好处: 可以调换顺序, 更加直观
```

