# 🐍 Python vs 🟨 JavaScript 常见语法糖对照表

## 1. 多值返回 / 解包

| 功能           | Python                          | JavaScript                                            |
| -------------- | ------------------------------- | ----------------------------------------------------- |
| 函数返回多个值 | `return a, b` → 实际是 `(a, b)` | 只能返回数组/对象：`return [a, b]` 或 `return {a, b}` |
| 解包赋值       | `x, y = foo()`                  | `const [x, y] = foo()`                                |

---

## 2. 展开 (Unpacking / Spread)

| 功能     | Python          | JavaScript                               |
| -------- | --------------- | ---------------------------------------- |
| 展开列表 | `add(*nums)`    | `add(...nums)`                           |
| 展开字典 | `greet(**info)` | `greet({...info})` 或 `fn(...objValues)` |

👉 对比：

* `*` ≈ `...` (list/array)
* `**` ≈ `...` (dict/object)

---

## 3. 推导式 / 映射

| 功能     | Python                    | JavaScript                                                   |
| -------- | ------------------------- | ------------------------------------------------------------ |
| 列表生成 | `[x*x for x in range(5)]` | `Array.from({length:5}, (_,x)=>x*x)` 或 `[...Array(5)].map((_,x)=>x*x)` |
| 字典生成 | `{k: v for k,v in pairs}` | `Object.fromEntries(pairs.map(([k,v]) => [k,v]))`            |
| 生成器   | `(x*x for x in range(5))` | `function*(){ for(let x=0;x<5;x++) yield x*x }`              |

---

## 4. 条件表达式 (三元运算符)

| Python                          | JavaScript                        |
| ------------------------------- | --------------------------------- |
| `res = "yes" if flag else "no"` | `const res = flag ? "yes" : "no"` |

---

## 5. with 上下文管理

| Python                         | JavaScript                                                   |
| ------------------------------ | ------------------------------------------------------------ |
| `with open("a.txt") as f: ...` | `await fs.promises.open("a.txt")` + `try/finally { file.close() }` （JS 没有原生 `with-as` 语法糖） |

---

## 6. 循环简写

| 功能       | Python                           | JavaScript                                       |
| ---------- | -------------------------------- | ------------------------------------------------ |
| 带索引循环 | `for i, x in enumerate(arr):`    | `arr.forEach((x, i) => {...})`                   |
| 多数组并行 | `for a, b in zip(list1, list2):` | `list1.forEach((a, i) => { let b = list2[i]; })` |

---

## 7. f-string / 模板字符串

| Python                           | JavaScript                            |
| -------------------------------- | ------------------------------------- |
| `f"Hello {name}, you are {age}"` | `` `Hello ${name}, you are ${age}` `` |

---

## 8. 布尔运算短路

| Python             | JavaScript                       |
| ------------------ | -------------------------------- |
| `a = a or default` | `a = a                           |
| `b = cond and val` | `b = cond && val`                |

---

## 9. 海象运算符

| Python                      | JavaScript                       |
| --------------------------- | -------------------------------- |
| `if (n := len(data)) > 10:` | `if ((n = data.length) > 10) {}` |

---

## ✅ 总结

* Python 的 `* / **` ≈ JS 的 `...`
* Python 的 `f"..."` ≈ JS 的 `` `...` ``
* Python 的 `or/and` ≈ JS 的 `||/&&`
* Python 的 **推导式** ≈ JS 的 **map/filter/reduce + Array.from**
* Python 的 `with` 在 JS 里需要 `try/finally` 来代替
* Python 的 `:=` 海象运算符 ≈ JS 的 `=` 赋值表达式
