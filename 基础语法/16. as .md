# 🐍 Python 中 `as` 用法 vs 🌐 JavaScript 对比

## 1️⃣ with 语句

```python
# Python
with open("data.txt", "r") as f:
    print(f.read())
# f = open("data.txt", "r") 的语法糖，自动调用 __enter__/__exit__
```

```javascript
// JavaScript 没有 with-as
// 一般用 try/finally 手动关闭资源
const fs = require("fs");
const f = fs.openSync("data.txt", "r");
try {
  let buf = Buffer.alloc(100);
  fs.readSync(f, buf, 0, 100, 0);
  console.log(buf.toString());
} finally {
  fs.closeSync(f);
}
```

---

## 2️⃣ import 起别名

```python
# Python
import numpy as np
from math import sqrt as square_root
print(np.arange(3))
print(square_root(16))
```

```javascript
// JavaScript
import * as np from "numpy";   // ES6 模块
import { sqrt as squareRoot } from "mathjs";
console.log(squareRoot(16));
```

---

## 3️⃣ 异常捕获

```python
# Python
try:
    1/0
except ZeroDivisionError as e:
    print("错误:", e)
```

```javascript
// JavaScript
try {
  throw new Error("boom");
} catch (e) {
  console.log("错误:", e.message);
}
```

⚠️ JS `catch` 里本身就是变量绑定，不需要 `as`。

---

## 4️⃣ 多重 with

```python
# Python
with open("a.txt") as f1, open("b.txt") as f2:
    data = f1.read() + f2.read()
```

```javascript
// JavaScript 没有类似语法
// 一般用多个 try/finally 嵌套
```

---

## 📌 总结对比表

| 场景          | Python `as`                | JavaScript 对应方式               |
| ----------- | -------------------------- | ----------------------------- |
| **with**    | `with obj as x:`           | ❌ 无，手写 `try/finally`          |
| **import**  | `import numpy as np`       | `import * as np from 'numpy'` |
| **except**  | `except Error as e:`       | `catch (e) {}`                |
| **多重 with** | `with A() as a, B() as b:` | ❌ 无，需多层 `try/finally`         |

👉 **结论**：

* Python 的 `as` 统一语义：把前面东西绑定到变量名
* JavaScript 没有单独的 `as` 关键字，但在 **import / catch** 有类似效果

```python
try:
    # 可能出错的代码
    risky_operation()
except ValueError as e:
    # 处理特定异常
    print("数值错误:", e)
except Exception as e:
    # 兜底处理
    print("未知错误:", e)
else:
    # 没出错才执行
    print("执行成功")
finally:
    # 不论如何都执行
    print("结束清理")

```

