# 🐍 Python 二进制数据 Demo

## 1️⃣ bytes（不可变二进制序列）

```python
b = b"ABC"
print("初始 bytes:", b)
print("索引 b[0]:", b[0])          # 65 (ASCII 'A')
print("切片 b[:2]:", b[:2])        # b'AB'

# 解码为字符串
print("decode:", b.decode("utf-8"))  # 'ABC'
```

---

## 2️⃣ bytearray（可变二进制序列）

```python
ba = bytearray(b"ABC")
print("初始 bytearray:", ba)

# 修改
ba[0] = 97
print("修改 ba[0]=97:", ba)  # bytearray(b'aBC')

# 增
ba.append(100)  # 添加 'd'
print("append:", ba)  # bytearray(b'aBCd')

ba.extend([101,102])  # 添加 'ef'
print("extend:", ba)  # bytearray(b'aBCdef')

# 删
ba.pop()
print("pop:", ba)

ba.remove(98)  # 删除 'B'
print("remove:", ba)

ba.clear()
print("clear:", ba)
```

---

## 3️⃣ memoryview（内存视图，零拷贝）

```python
b = b"ABC"
mv = memoryview(b)

print("memoryview 对象:", mv)
print("索引 mv[0]:", mv[0])
print("转列表:", list(mv))        # [65,66,67]
print("tobytes:", mv.tobytes())  # b'ABC'
```

---

## 📌 API 对比表

| 类型         | 可变性   | 示例                     | 常用 API / 操作                                              |
| ------------ | :------- | ------------------------ | ------------------------------------------------------------ |
| `bytes`      | ❌ 不可变 | `b = b"ABC"`             | `b[i]`, `b[a:b]`, `.decode()`                                |
| `bytearray`  | ✅ 可变   | `ba = bytearray(b"ABC")` | `.append()`, `.extend()`, `.insert()`, `.pop()`, `.remove()`, `.clear()` |
| `memoryview` | ❌ 视图   | `mv = memoryview(b)`     | `.tobytes()`, `.tolist()`, `.cast()`                         |

---

## 📌 应用场景

* **bytes** → 网络传输、文件存储（不可变更安全）
* **bytearray** → 需要修改的缓冲区，例如协议解析
* **memoryview** → 大数据处理时减少拷贝，提高效率

