# 🐍 Python vs 🌐 JavaScript 类型判断对比



| 功能     | Python                      | JavaScript                 | 说明                                                  |                        |                           |
| ------ | --------------------------- | --------------------------- | --------------------------------------------------- | ---------------------- | ------------------------- |
| 获取精确类型 | `type(obj)`                 | `typeof obj`                | Python 返回类对象，JS 返回字符串（不够精细）                         |                        |                           |
| 严格比较类型 | `type(x) is int`            | `typeof x === "number"`     | Python 区分类，JS 有时不准（如 `typeof null === "object"`)    |                        |                           |
| 继承判断   | `isinstance(obj, Class)`    | `obj instanceof Class`      | Python 会考虑继承，JS 基于原型链                               |                        |                           |
| 子类判断   | `issubclass(Sub, Base)`     | 无直接语法                       | Python 原生支持，JS 需要手写 `Sub.prototype instanceof Base` |                        |                           |
| 是否可调用  | `callable(obj)`             | `typeof obj === "function"` | Python 看是否实现 `__call__`，JS 看是否函数                    |                        |                           |
| 鸭子类型   | 调用对象的方法（不关心类型）              | JS 也常直接调用方法                 | Python 官方鼓励 Duck Typing，JS 也常用                      |                        |                           |
| 类型检查容错 | `isinstance(x, (int, str))` | `typeof x === "string"      |                                                     | typeof x === "number"` | Python 可以传元组批量检查，JS 要手写逻辑 |||
| 未定义    | 访问未定义变量报 `NameError`        | 返回 `undefined`              | Python 更严格，JS 更宽松                                   |                        |                           |

---

## 📌 示例对比

### Python

```python
x = 42
print(type(x))             # <class 'int'>
print(isinstance(x, int))  # True

class Animal: pass
class Dog(Animal): pass
print(issubclass(Dog, Animal))  # True

print(callable(len))       # True
```

### JavaScript

```javascript
let x = 42;
console.log(typeof x);         // "number"
console.log(x instanceof Number); // false (因为不是对象包装类型)

class Animal {}
class Dog extends Animal {}
console.log(new Dog() instanceof Animal); // true

console.log(typeof function(){} === "function"); // true
```

---

## ✅ 总结差异

1. **Python 类型系统更严格**：未定义变量直接报错，类型检查更精确
2. **JS 类型系统更宽松**：`typeof` 粗糙，`null`/`array` 等都有坑
3. **Python 提供丰富 API**：`isinstance` / `issubclass` / `callable` / Duck Typing
4. **JS 更依赖原型链**：`instanceof` 基于原型，不如 Python 直观
5. **Duck Typing**：两者都用，但 Python 更强调“只要像鸭子就是鸭子”

---
