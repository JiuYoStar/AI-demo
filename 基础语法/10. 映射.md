好的 ✅ 我来帮你整理一下 **Python 字典（dict）的常用 API**，分类清晰：

---

# 🐍 Python `dict` 常用 API 速查表

## 1️⃣ 创建

```python
d = {"a": 1, "b": 2}
d2 = dict(c=3, d=4)
d3 = dict([("e", 5), ("f", 6)])
```

---

## 2️⃣ 增 & 改

```python
d["x"] = 10              # 新增或修改
d.update({"y": 20})      # 批量更新
# 常用作初始化
d.setdefault("z", 99)    # 如果 key 不存在就设置，存在则不变
```

---

## 3️⃣ 删

```python
d.pop("a")               # 删除并返回指定 key 的值，不存在时报错
d.pop("notfound", None)  # 不存在返回默认值
d.popitem()              # 随机删除并返回一个 (key, value) 对
del d["b"]               # 删除指定 key
d.clear()                # 清空字典
```

---

## 4️⃣ 查

```python
print(d["x"])            # 取值，key 不存在时报错
print(d.get("x"))        # 取值，不存在返回 None
print(d.get("no", 0))    # 取值，不存在返回默认值
```

---

## 5️⃣ 遍历

```python
for k in d.keys():       # 遍历 key
    print(k)

for v in d.values():     # 遍历 value
    print(v)

for k, v in d.items():   # 遍历 (key, value)
    print(k, v)
```

---

## 6️⃣ 复制

```python
d2 = d.copy()            # 浅拷贝
```

---

## 7️⃣ 其他方法

```python
len(d)                   # 字典长度
"x" in d                 # 判断 key 是否存在
d.keys()                 # 所有 key 的视图
d.values()               # 所有 value 的视图
d.items()                # 所有 (key,value) 的视图
```

---

## 📌 总结常用方法对比表

| 分类     | 方法/语法                                  | 说明       |
| ------ | -------------------------------------- | -------- |
| **增改** | `d[k]=v`, `update()`, `setdefault()`   | 新增/修改键值对 |
| **删**  | `pop()`, `popitem()`, `del`, `clear()` | 删除元素     |
| **查**  | `d[k]`, `get()`                        | 获取值      |
| **遍历** | `keys()`, `values()`, `items()`        | 遍历字典     |
| **其他** | `copy()`, `len()`, `in`                | 工具方法     |

---



| 分类         | 方法 / 语法                | 说明                                   | 示例                                              |
| ------------ | -------------------------- | -------------------------------------- | ------------------------------------------------- |
| **创建**     | `{}` / `dict()`            | 创建字典                               | `d = {"a":1}` / `d = dict(a=1)`                   |
| **查**       | `d[k]`                     | 获取值，key 不存在报错                 | `d["a"] → 1`                                      |
|              | `d.get(k, default)`        | 获取值，key 不存在返回 `default`       | `d.get("x", 0)`                                   |
|              | `d.keys()`                 | 返回所有 key 视图                      | `list(d.keys())`                                  |
|              | `d.values()`               | 返回所有 value 视图                    | `list(d.values())`                                |
|              | `d.items()`                | 返回 (key, value) 视图                 | `list(d.items())`                                 |
|              | `k in d`                   | 判断 key 是否存在                      | `"a" in d`                                        |
| **增 / 改**  | `d[k] = v`                 | 新增或修改键值                         | `d["b"] = 2`                                      |
|              | `d.update(d2)`             | 批量更新 / 合并                        | `d.update({"c":3})`                               |
|              | `d.setdefault(k, v)`       | 若 k 存在返回原值，否则设置默认值      | `d.setdefault("x", 0)`                            |
| **删**       | `d.pop(k, default)`        | 删除并返回值，不存在则返回 default     | `d.pop("a", None)`                                |
|              | `d.popitem()`              | 删除并返回一个 (k,v)，通常是最后插入的 | `d.popitem()`                                     |
|              | `del d[k]`                 | 删除指定 key                           | `del d["a"]`                                      |
|              | `d.clear()`                | 清空字典                               | `d.clear()`                                       |
| **复制**     | `d.copy()`                 | 浅拷贝（子对象共享）                   | `shallow = d.copy()`                              |
|              | `copy.deepcopy(d)`         | 深拷贝（完全独立）                     | `deep = deepcopy(d)`                              |
| **只读视图** | `MappingProxyType(d)`      | 返回字典的只读代理                     | `proxy = MappingProxyType(d)`                     |
| **迭代**     | `for k in d`               | 遍历 key                               | `for k in d: ...`                                 |
|              | `for v in d.values()`      | 遍历值                                 | `for v in d.values(): ...`                        |
|              | `for k,v in d.items()`     | 遍历键值对                             | `for k,v in d.items(): ...`                       |
| **其他**     | `len(d)`                   | 返回dict中元素个数                     | `len(d)`                                          |
|              | `dict.fromkeys(keys, val)` | 生成字典，所有 key 共享同一个默认值    | `dict.fromkeys("abc", 0)` → `{'a':0,'b':0,'c':0}` |
