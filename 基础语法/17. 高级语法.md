# 🐍 Python 高频高级语法清单

## 1️⃣ 推导式 (Comprehensions)

* **列表推导式**：`[x*x for x in range(10) if x%2==0]`
* **集合推导式**：`{x for x in range(10)}`
* **字典推导式**：`{x: x*x for x in range(5)}`
* **生成器表达式**：  `(x*x for x in range(10))`

---

## 2️⃣ 迭代器 & 生成器

* **生成器函数**：`yield`
* **生成器表达式**：`(x for x in data)`
* **`next()` 手动迭代**
* **`iter()` 转迭代器**

---

## 3️⃣ 装饰器 (Decorator)

```python
def log(func):
    def wrapper(*args, **kwargs):
        print("call", func.__name__)
        return func(*args, **kwargs)
    return wrapper

@log
def hello():
    print("hi")
```

---

## 4️⃣ 上下文管理器 (with)

```python
with open("file.txt") as f:
    data = f.read()
```

* 自定义：实现 `__enter__` / `__exit__`

---

## 5️⃣ 解包 (Unpacking)

```python
a, b, *rest = [1, 2, 3, 4, 5]
print(rest)  # [3, 4, 5]

d = {"x":1, "y":2}
print({**d, "z":3})  # {'x':1,'y':2,'z':3}
```

---

## 6️⃣ Lambda & 高阶函数

* 匿名函数：`lambda x: x*x`
* `map / filter / reduce / sorted(key=...)`

---

## 7️⃣ 装饰性语法

* **f-string**：`f"{x:.2f}"`
* **海象运算符**：`if (n := len(data)) > 10:`
* **链式比较**：`1 < x < 10`

---

## 8️⃣ 面向对象扩展

* **魔法方法** (`__str__`, `__len__`, `__getitem__`, `__call__`)
* **`@property` 属性装饰器**
* **`@classmethod` / `@staticmethod`**

---

## 9️⃣ 异步编程

* **async / await**
* **async for / async with**
* 常用于 Web 框架 (FastAPI, aiohttp)

---

## 🔟 其他常用高级语法

* **enumerate**：`for i, val in enumerate(lst): ...`
* **zip**：`for a, b in zip(list1, list2): ...`
* **collections 高级容器**：`defaultdict`, `Counter`, `namedtuple`
* **typing 类型注解**：`def f(x: int) -> str:`
* **contextlib**：`@contextmanager` 简化 with

---

## ✅ 总结

高频高级语法主要包括：

* 推导式
* 生成器 / 迭代器
* 装饰器
* with 上下文
* 解包
* lambda / map / filter
* f-string & 海象运算符
* OOP 魔法方法 / property
* async/await 异步



```python
```

