# 虚拟环境故障排查

## 🐛 问题: flask命令找不到

### 症状

```bash
(venv) ➜ demo flask run
zsh: command not found: flask
```

虽然提示符显示 `(venv)`,但flask命令仍然找不到。

## 🔍 诊断步骤

### 1. 检查虚拟环境是否真正激活

```bash
# 检查VIRTUAL_ENV环境变量
echo $VIRTUAL_ENV
# 应该输出: /Users/lingk/work/py/demo/test/venv

# 如果输出为空或其他路径,说明虚拟环境没有正确激活
```

### 2. 检查Python路径

```bash
which python
# 应该输出: /Users/lingk/work/py/demo/test/venv/bin/python

# 如果输出是系统路径(如/usr/bin/python),说明虚拟环境没激活
```

### 3. 检查flask文件是否存在

```bash
ls -la /Users/lingk/work/py/demo/test/venv/bin/flask
# 应该显示flask文件存在
```

### 4. 检查PATH环境变量

```bash
echo $PATH | tr ':' '\n' | grep flask-demo1
# 应该包含: /Users/lingk/work/py/demo/test/venv/bin
```

## 🔧 解决方案

### 方案1: 重新激活虚拟环境

```bash
# 1. 先停用(如果已激活)
deactivate

# 2. 重新激活
source /Users/lingk/work/py/demo/test/venv/bin/activate

# 3. 验证
which flask
# 应该输出: /Users/lingk/work/py/demo/test/venv/bin/flask

# 4. 测试
flask --version
```

### 方案2: 使用绝对路径

```bash
# 直接使用flask的绝对路径
/Users/lingk/work/py/demo/test/venv/bin/flask run

# 或使用python -m方式
python -m flask run
```

### 方案3: 重新创建虚拟环境

```bash
# 1. 备份requirements.txt
cp requirements.txt requirements.txt.bak

# 2. 删除旧的虚拟环境
cd /Users/lingk/work/py/demo/test
rm -rf venv

# 3. 创建新的虚拟环境
python -m venv venv

# 4. 激活虚拟环境
source venv/bin/activate

# 5. 安装依赖
pip install -r requirements.txt

# 6. 验证
which flask
flask --version

# 7. 运行
flask run
```

### 方案4: 检查activate脚本

```bash
# 查看activate脚本是否正确
cat venv/bin/activate | grep VIRTUAL_ENV

# 应该包含类似:
# VIRTUAL_ENV='/Users/lingk/work/py/demo/test/venv'

# 如果路径还是demo1,需要重新创建虚拟环境
```

## 🎯 推荐的完整修复流程

```bash
# 1. 进入项目目录
cd /Users/lingk/work/py/demo/test

# 2. 确保虚拟环境存在
ls -la venv/bin/activate

# 3. 完全重新激活
deactivate 2>/dev/null  # 忽略错误
source venv/bin/activate

# 4. 验证环境变量
echo "VIRTUAL_ENV: $VIRTUAL_ENV"
echo "Python路径: $(which python)"
echo "Flask路径: $(which flask)"

# 5. 测试flask命令
flask --version

# 6. 运行应用
flask run
```

## 📋 终端自动激活问题

如果IDE终端自动激活不工作:

### 检查配置

```bash
# 查看.vscode/settings.json
cat .vscode/settings.json | grep -A2 "defaultInterpreterPath"

# 应该是:
# "python.defaultInterpreterPath": "/Users/lingk/work/py/demo/test/venv/bin/python",
```

### 重新加载窗口

```
Cmd + Shift + P → Developer: Reload Window
```

### 手动选择解释器

```
Cmd + Shift + P → Python: Select Interpreter
→ 选择: /Users/lingk/work/py/demo/test/venv/bin/python
```

## 🔧 临时解决方案

如果上述方法都不行,可以使用:

```bash
# 方式1: 使用python -m
cd /Users/lingk/work/py/demo/test
source venv/bin/activate
python -m flask run

# 方式2: 使用绝对路径
/Users/lingk/work/py/demo/test/venv/bin/flask run

# 方式3: 使用python run.py
python run.py
```

## ✅ 验证清单

- [ ] 虚拟环境存在: `ls venv/bin/flask`
- [ ] 虚拟环境已激活: `echo $VIRTUAL_ENV`
- [ ] Python路径正确: `which python`
- [ ] Flask已安装: `pip show flask`
- [ ] Flask命令可用: `which flask`
- [ ] Flask版本正确: `flask --version`

全部✅后,`flask run` 应该就能正常工作了!

## 🎯 最简单的方法

```bash
# 在终端中依次执行:
cd /Users/lingk/work/py/demo/test
deactivate
source venv/bin/activate
flask run
```

如果还是不行,使用:

```bash
python run.py
```

这个肯定能用! 🚀

