# Flask 启动命令参考

## ⚠️ 注意大小写!

```bash
# ❌ 错误 - 大写F
Flask run

# ✅ 正确 - 小写f
flask run
```

## 🚀 正确的启动命令

### 方式1: 使用 Flask CLI (推荐)

```bash
cd /Users/lingk/work/py/demo/test
source venv/bin/activate
flask run
```

### 方式2: 使用 Python

```bash
cd /Users/lingk/work/py/demo/test
source venv/bin/activate
python run.py
```

## 🔍 验证虚拟环境

```bash
# 1. 激活虚拟环境
source /Users/lingk/work/py/demo/test/venv/bin/activate

# 2. 检查提示符
# 应该显示: (venv) ➜ demo

# 3. 检查flask命令
which flask
# 输出: /Users/lingk/work/py/demo/test/venv/bin/flask

# 4. 检查flask版本
flask --version
# 输出: Flask 3.1.2
```

## 📋 常用命令 (注意小写)

```bash
# 运行应用
flask run

# 调试模式
flask run --debug

# 指定端口
flask run -p 8080

# 指定主机
flask run -h 0.0.0.0

# 查看路由
flask routes

# 进入Shell
flask shell
```

## 🐛 常见错误

### 错误1: 大小写错误

```bash
# ❌ 错误
Flask run
# 提示: zsh: command not found: Flask

# ✅ 正确
flask run
```

### 错误2: 虚拟环境未激活

```bash
# ❌ 错误
flask run
# 提示: command not found 或 使用了系统的flask

# ✅ 正确
source venv/bin/activate  # 先激活
flask run                 # 再运行
```

### 错误3: 路径错误

```bash
# ❌ 错误 - 在错误的目录
cd /Users/lingk/work/py/demo
flask run

# ✅ 正确 - 在项目目录
cd /Users/lingk/work/py/demo/test
flask run
```

## 🎯 快速启动脚本

创建一个启动脚本方便使用:

```bash
# 创建 start.sh
cat > start.sh << 'EOF'
#!/bin/bash
cd /Users/lingk/work/py/demo/test
source venv/bin/activate
flask run
EOF

# 添加执行权限
chmod +x start.sh

# 使用
./start.sh
```

## 📝 一行命令启动

```bash
cd /Users/lingk/work/py/demo/test && source venv/bin/activate && flask run
```

## ✅ 成功启动的标志

```
app app/__init__.py

============================================================
🔍 自动扫描并注册蓝图...
============================================================
...
✨✨✨ 蓝图注册完成! 共注册 X 个蓝图
...

 * Serving Flask app 'app'
 * Debug mode: on
 * Running on http://127.0.0.1:5000
Press CTRL+C to quit
```

---

**记住: 命令是小写 `flask`,不是大写 `Flask`!** 🎯

